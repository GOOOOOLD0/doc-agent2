# Book Ingestion Tools

本目录用于存放书籍解析与 Wiki 构建工具。

计划包含：

- `parse_book.py`：解析原始书籍文件；
- `split_book.py`：按章节、条款或小节切分文本；
- `build_source_notes.py`：根据切分结果生成 Source Notes；
- `compile_wiki.py`：生成正式 Wiki 知识页。

当前目录为预留结构。具体工具由项目成员按照
`config/book_ingestion/` 中的配置逐步实现。

